/** Public surface for the offline / idle progress layer (M21). */

export type { OfflineOptions, OfflineReport } from "./offline";
export {
  simulateElapsed,
  DEFAULT_OFFLINE_CEILING_MS,
  OFFLINE_TICK_STEP_MS,
} from "./offline";
